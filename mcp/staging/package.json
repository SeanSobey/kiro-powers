{
  "name": "staging-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server that exposes staging directory paths for Docker-mounted powers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  }
}
